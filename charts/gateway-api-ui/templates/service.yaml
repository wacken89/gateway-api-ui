apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-api-ui.fullname" . }}
  labels:
    {{- include "gateway-api-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gateway-api-ui.selectorLabels" . | nindent 4 }}
